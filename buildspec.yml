version: 0.2
phases:
 pre_build:
   commands:
   - echo "Entered pre-build phase..."
 build:
   commands:
   - echo "Entered build phase..."
 post_build:
   commands:
   - echo "Entered build post-build phase..."
artifacts:
 files:
 - buildspec.yml
 - SampleWebApp.war